import Admin from '@/components/admin/Admin';

export const metadata = { title: 'Painel · La Ville Burger', robots: { index: false, follow: false } };

export default function Page() {
  return <Admin />;
}
